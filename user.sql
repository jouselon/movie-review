create table user (

userId varchar(20),
userPassword varchar(20),
userName varchar(20),
userEmail varchar(50),
primary key(userID)

);

insert into user values('admin','admin','°ü¸®ÀÚ','admin@admin.com');

select * from user;